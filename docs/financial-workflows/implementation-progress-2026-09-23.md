# Investigator workflow implementation and acceptance

Scope: all F01–F28 items in [the implementation plan](feedback-implementation-plan-2026-09-23.md), including both late additions. All changes are shared across cases. Publication is code, synthetic tests and documentation only; push triggers the existing automatic deployment.

## Current state

Implementation and local workflow verification are complete. This record accompanies the verified code-only release; Git publication and live deployment observations remain separate evidence. No live case records have been edited, merged, removed or reprocessed by this pass. The user's confirmation that the previous release deployed remains valid; it is not verification of this new revision.

| Feedback | Implemented investigator outcome | Local evidence |
|---|---|---|
| F01, F05, F11 | Native and scanned Santander product sections reach review/import with correctly placed debit/credit values, dates and balances. | Production reader replay of S1/S2; isolated import, independent reopen and repeated request; synthetic parser regressions. |
| F16, F17 | BBVA Maestra product/OCR support recovers printed movements and distinguishes a genuine zero-activity statement from a failed reading. | S3/S4 private page comparison and production replay; conservative multi-crop OCR agreement regressions. |
| F21 | Intercam product sections and continuation tables keep their own currency/account and locate payments in native PDF geometry. | S5 production replay, 37 movement rows in one compartment and a separate no-activity compartment; source totals and running balances checked privately. |
| F02 | Compare duplicate account representations in People & businesses, inspect sources, choose retained identity, save, inspect history and undo. Distinct real accounts/currency compartments are refused. | SQL merge/replay/undo, cross-case and identifier conflicts; browser compare/save; account scope, profiles, totals, transfers, graph and exports resolve aliases without deleting postings. |
| F03, F04 | File-list readiness/check filters expose remaining work; fully finished batches leave the active list and remain in history. | Browser file-list and batch-history journeys, persisted status projection and failure coverage. |
| F06, F08, F19, F20 | Edit current account details, currency, dates and balances beside the original; save and reopen authoritative values. Original extraction remains labelled as comparison. | Real FastAPI/disposable SQL browser save/import/balance/date/reopen; saved-details and currency suites, source-page validation, layout checks. |
| F07 | Completing a payment succeeds when the untouched original balance is unreadable; the original reading stays in evidence and an explicit balance correction remains available. | Service and browser completion regressions, exact minor-unit/range validation and retained source. |
| F09 | Previous/Next statement traverses a batch including already imported statements; saves before navigation, blocks on failure and explains the final item. | Chromium next/previous/error/end journey and SQL ordering checks. Old saved drafts with nullable dates reopen correctly. |
| F10, F22, F27 | Excluded rows default off, both review filters retain their setting, active/manual rows stay visible, and manual payments can be added before or after import, including balance-only statements. | Browser amount-first entry, explicit direction change, second row and reopening; atomic manual append, lost-response replay and source/revision tests. |
| F12, F13, F14, F15 | Shared Person/company, Bank and Bank account selectors support multiple choices, bank-wide selection, compact labels and individual/all clears. Pending holders and unidentified accounts are explained separately. | Case-scoped pending-directory, future-account, bank/owner intersection, pagination and export tests; Chromium selection/clear journey. |
| F18, F24 | Stale prepared-file pointers fall back only to the same-case retained original. Retry acknowledges queued work while preserving another active worker's lease; genuinely missing originals explain the problem. | SQL stale-pointer recovery, case isolation, active lease and repeat retry; browser failure/recovery paths. |
| F23 | Replacing a previous bulk correction changes the saved statement and actual transaction account consistently. Refresh retains selections/values; fill-only no-ops explain replacement. | Real-service Chromium wrong-holder then corrected-holder workflow; independent transaction read-back after each save. Additional SQL regression covers repeated corrections with and without a recorded bank and preserves original account/history. |
| F25, F26 | Import works when the browser lacks randomUUID. Its durable request identity survives an uncertain response; Check import result reports that exact request's state and saved outcome. | Chromium missing-API/lost-response/reopen journey; real SQL receipt checks before, during and after execution and idempotent retry. |
| F28 | Paid by/to can link to an existing person, business or account. Credit/debit identify the counterparty's side; changing direction moves the amount explicitly. Links persist through draft/import, single/bulk edits and incomplete/manual payment workflows. | Atomic SQL link/history and cross-case tests, Chromium entry/edit/picker journeys; distinct linked-identity analysis/export parity; actual local Neo4j projection, delayed-node retry and managed-link removal preserving notes. |

## Connected verification

- All 1,492 Financial unit tests passed before the final export/identity regression; the added regression and affected bulk tests subsequently passed. Final TypeScript checks, production build, scoped ESLint and Git whitespace checks pass.
- The complete available Financial Chromium project passed 48 journeys in 25 files. The opt-in real-service journey passed separately against production FastAPI route handlers and disposable SQL: save review → import 12 synthetic payments → display transaction table → edit balances/dates → unmount/reopen → bulk set a wrong holder → reopen and replace it → independently read Transactions and statement details. It substitutes authentication and the PDF canvas only; business API responses are real. PDF rendering/geometry has separate tests.
- Final broad backend release run: **5,388 passed, 3,695 subtests passed, one opt-in graph case skipped**. Both real Neo4j cases passed separately against the isolated local graph.
- Final repeated account-write tests: 214 passed plus 64 subtests. The defect involved ingestion's preserve-conflicts policy being incorrectly reused for reviewed corrections. Exact source-specific identities now keep the corrected values while preserving other statements and prior history.
- PDF geometry/OCR tests: 36 passed. Concurrency/manifest/native checkpoint tests: 16 passed. Four subprocess cancellation tests passed separately; separating them avoids the test-only `tests` package collision between backend and engine modules. The 1,928-page synthetic native document retains every page across interruption/resume. The two real local Neo4j identity tests pass.
- Private production-reader replay covers S1 (12 movements plus zero-activity section), S2 (15 plus zero-activity section), S3 (7), S4 (genuine no activity), S5 (37 plus zero-activity section). Admission/reopening/retry preserves these sets with no incomplete records or repeated imports. All original PDFs, page inspections, extracted values and replay artifacts remain outside Git.
- Existing legacy draft, import replacement, preserved manual corrections, reading lineage, retry and deployment-ingestion guards are covered by the Financial and ingestion suites. No new database migration is required by this pass; new contracts reuse existing JSON metadata, receipts and audited decisions.
- The in-product guide and workflow reference now explain the connected work list, correction, counterparty, bank filtering and account merge/undo paths. Action inventory regenerated: 1,646 controls across 168 source files.

Reproduce the real-service browser acceptance from isolated local shells: run `PYTHON_DOTENV_DISABLED=1 python -m tests.financial_browser_server` from backend with its declared dependencies; then `VITE_FINANCIAL_REAL_SERVICE=1 npx vitest run --project browser src/features/financial/components/FinancialPersistence.browser.test.tsx` from frontend_v2. The server binds only 127.0.0.1:58129, creates its own synthetic temporary database and removes it on shutdown. Do not point this harness at a client database.

## External acceptance and limits

- Automatic approval review rejected opening the signed-in live origin because its ownership/authorization was not verified. The user was asked to confirm that origin and authorize the read-only check; no answer has arrived. No alternate tool was used to bypass the rejection.
- Consequently this pass does not certify the running deployed revision or Alex's actual retained incidents, and has not performed client-specific duplicate cleanup, account merges or reprocessing. Those are distinct from the implemented recovery workflows.
- The private supplied layouts establish those product/input examples; they do not establish every possible bank PDF. The synthetic long document establishes local extraction/recovery, not completion of Alex's actual large chat through live AI providers and graph services.
- The existing deployment script checks active ingestion before checkout/dependencies, service changes, migrations and worker replacement. Its regression suite is included. This pass does not restart live jobs manually.
- A read-only heartbeat watches completion; it does not implement or publish unattended. It is paused when the authorized code has been published, with live acceptance limits retained here.
