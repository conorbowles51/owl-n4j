# Statement ingestion workflow plan

The plan is now explicitly platform-wide: [statement ingestion, correction and import workflow plan](statement-ingestion-workflow-plan.md).

BNB2 is the case reproducing Alex's feedback. All requirements apply to existing and future Loupe Financial cases and ingestion paths, including prevention of internally generated duplicate work or counted payments. This link is retained so earlier references continue to work.
