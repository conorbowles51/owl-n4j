[pytest]
# Only collect the real test suite. backend/scripts/ contains ad-hoc
# test_*.py probes that call sys.exit() at import time.
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    acceptance: fixture-based acceptance tests for the citation / AI-limitation contract (DKT-413).
