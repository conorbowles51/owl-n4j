# Test-only dependencies. Kept out of requirements.txt so the runtime image
# does not grow. Install with:
#   ../.venv/bin/pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.4.2
pytest-asyncio==1.2.0
