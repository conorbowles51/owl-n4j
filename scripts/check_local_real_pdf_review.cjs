// Deliberate two-row acceptance sample, visually checked on source PDF page 4.
// Writes only into the isolated case created by check_local_pdf_intake_ui.cjs.
const path=require('path'),fs=require('fs');const root=path.resolve(__dirname,'..');const {chromium}=require(path.join(root,'frontend_v2/node_modules/playwright'));
const output=path.join(root,'data/local-runtime/real-pdf-review-check.json');
(async()=>{if(fs.existsSync(output))throw Error('Review record exists; inspect before retrying any write');const intake=JSON.parse(fs.readFileSync(path.join(root,'data/local-runtime/real-pdf-intake-check.json')));if(intake.case_id!=='c06c264c-a402-4895-a696-be634fe23629')throw Error('Unexpected acceptance case');
 const report={case_id:intake.case_id,status:'started'};const save=()=>fs.writeFileSync(output,JSON.stringify(report,null,2));const browser=await chromium.launch({headless:true});try{
 const page=await browser.newPage({viewport:{width:1600,height:1100}});page.setDefaultTimeout(30000);await page.goto('http://127.0.0.1:55174/login');await page.getByPlaceholder('Enter your username').fill('loupe-local@example.com');await page.getByPlaceholder('Enter your password').fill('Loupe-local-test-2026');await page.getByRole('button',{name:'Sign in',exact:true}).click();await page.waitForURL(u=>!u.pathname.includes('login'));
 await page.goto(`http://127.0.0.1:55174/cases/${intake.case_id}/financial`);await page.getByRole('button',{name:'Open PDF readings',exact:true}).click();await page.getByRole('button',{name:'Choose PDF rows',exact:true}).click();await page.getByRole('button',{name:`${intake.filename} · page 4`,exact:true}).click();
 for(const [col,meaning] of [[1,'transaction_date'],[3,'description'],[4,'amount']])await page.getByLabel(`Column ${col} meaning`,{exact:true}).selectOption(meaning);
 await page.getByRole('button',{name:'Next table rows',exact:true}).click();for(const n of [41,42])await page.getByLabel(`Select source row ${n}`,{exact:true}).check();
 const mappingResponse=page.waitForResponse(r=>r.url().includes('/candidate-mappings?')&&r.request().method()==='POST');save();await page.getByRole('button',{name:'Save selected rows for review',exact:true}).click();const mapped=await mappingResponse;const mapping=await mapped.json();if(!mapped.ok()||mapping.candidates.length!==2)throw Error('Unexpected mapping outcome');report.mapping_id=mapping.id;report.candidate_ids=mapping.candidates.map(c=>c.id);report.status='mapped';save();
 for(const [i,n,amount,date,description] of [[0,41,'14.00','2021-04-22',"TST' ZEBA WASHINGTON DC"],[1,42,'100.00','2021-04-23',"TST' THE GRILL WASHINGTON DC"]]){
  await page.getByRole('button',{name:`Review source row ${n}`,exact:true}).click();await page.getByLabel('Currency',{exact:true}).fill('USD');
  if(i===0){await page.getByRole('button',{name:'Set up a provisional account',exact:true}).click();await page.getByLabel('Provisional account label',{exact:true}).fill('LOCAL SAMPLE — Merrick statement page 4');await page.getByLabel('Reason for provisional account',{exact:true}).fill('Isolated two-row acceptance sample. Source page 4 is a US credit-card statement; this provisional account does not establish wider account identity.');const pending=page.waitForResponse(r=>r.url().includes('/provisional-account?')&&r.request().method()==='POST');await page.getByRole('button',{name:'Create provisional account',exact:true}).click();const response=await pending;const account=await response.json();if(!response.ok())throw Error('Account setup failed');report.account_id=account.account.id;save();}
  else await page.getByLabel('Account',{exact:true}).selectOption(report.account_id);
  await page.getByLabel('Reviewed amount',{exact:true}).fill(amount);await page.getByLabel('Direction',{exact:true}).selectOption('debit');await page.getByLabel('Transaction date',{exact:true}).fill(date);await page.getByLabel('Description',{exact:true}).fill(description);await page.getByLabel('Reason for decision',{exact:true}).fill('Isolated acceptance: visually checked original PDF page 4 purchase row and amount. Year 2021 from the same statement header; USD context from US issuer and printed dollar amounts. Description checked against image, correcting OCR OC to DC. Two selected rows only; no complete-statement verification.');
  const source=page.getByRole('region',{name:'Original document beside review',exact:true});await source.getByRole('button',{name:'Source column 4: amount',exact:true}).click();const image=source.getByAltText('Page 4 of the source document',{exact:true});await image.waitFor();await image.evaluate(img=>img.decode());await source.evaluate(el=>el.scrollIntoView({block:'start'}));if(i===0)await page.screenshot({path:'/tmp/loupe-neilbyrne-real-row-review.png'});
  const pending=page.waitForResponse(r=>r.url().includes('/review?')&&r.request().method()==='POST');await page.getByRole('button',{name:'Record resolved reading',exact:true}).click();const response=await pending;const review=await response.json();if(!response.ok()||review.status!=='resolved'||review.reading.amount_minor!==(i===0?'1400':'10000'))throw Error('Review failed '+JSON.stringify(review));report['review_'+i]=review.candidate_id;save();await page.getByRole('button',{name:'Close review',exact:true}).click();
 }
 await page.getByRole('button',{name:'Preview finalization',exact:true}).click();await page.getByRole('checkbox',{name:/These are documentary/}).check();await page.getByRole('checkbox',{name:/I accept incomplete/}).check();await page.getByLabel('Reason for finalization').fill('Isolated acceptance sample: only two visually reviewed purchase rows from page 4. Incomplete extraction coverage is accepted for this test; retain P3 exclusion from verified totals.');
 const finalized=page.waitForResponse(r=>r.url().includes('/finalize?')&&r.request().method()==='POST');await page.getByRole('button',{name:'Finalize selected rows',exact:true}).click();const response=await finalized;report.finalization=await response.json();save();if(!response.ok())throw Error('Finalization failed');await page.getByText(/Finalized 2 transactions/).waitFor();report.status='finalized';save();
 const download=page.waitForEvent('download');await page.getByRole('button',{name:'Download ledger snapshot',exact:true}).click();await(await download).saveAs(path.join(root,'data/local-runtime/real-pdf-sample-export.zip'));report.export_downloaded=true;save();console.log(JSON.stringify(report));
 }finally{await browser.close()}})().catch(e=>{console.error(e);process.exitCode=1});
